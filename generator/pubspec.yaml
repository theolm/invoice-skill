name: invoice_generator
description: CLI tool to generate invoice PDFs.
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  args: ^2.4.0
  pdf: ^3.10.8
  intl: ^0.19.0

dev_dependencies:
  lints: ^3.0.0
